{
  "name": "@damwha/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Wire enums shared by damwha-be and damwha-fe. The single source of truth for values both sides must agree on.",
  "type": "commonjs",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/cjs/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/cjs/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.cjs.json && tsc -p tsconfig.esm.json && node -e \"require('fs').writeFileSync('dist/esm/package.json', JSON.stringify({type:'module'}))\"",
    "prepare": "pnpm run build"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
