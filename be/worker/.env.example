DATABASE_URL=postgres://postgres:postgres@localhost:5432/damwha
STORAGE_ROOT=../storage
WORKER_ID=worker-1
HF_TOKEN=
POLL_INTERVAL_SECONDS=2
HEARTBEAT_INTERVAL_SECONDS=30
STT_CHUNK_MINUTES=25
MODEL_CACHE_DIR=

# Search / embed service
SEARCH_EMBEDDING_MODEL=BAAI/bge-m3
SEARCH_EMBEDDING_DIM=1024
EMBED_SERVICE_HOST=127.0.0.1
EMBED_SERVICE_PORT=8100

# Lens extraction LLM — any loopback OpenAI-compatible server.
# mlx_lm.server loads the HF repo directly (no Ollama); see SMOKE.md.
#   mlx_lm.server --model mlx-community/Qwen3-4B-Instruct-2507-8bit --port 8000
LENS_LLM_BASE_URL=http://127.0.0.1:8000/v1
LENS_LLM_MODEL=mlx-community/Qwen3-4B-Instruct-2507-8bit
LENS_LLM_TIMEOUT_SECONDS=300
# 렌즈/요약 공통 생성 길이 상한. 서버 기본값(mlx_lm.server는 512)에 맡기면 JSON이
# 중간에서 잘려 llm_invalid_response로 실패한다 — 넉넉히 잡는다.
LENS_LLM_MAX_TOKENS=8192
# Conversation summary reuses LENS_LLM_BASE_URL but carries its own model name —
# leaving it unset stamps the Ollama-shaped default into the job payload and the
# request 404s on mlx_lm.server. Set it whenever LENS_LLM_MODEL is set.
SUMMARY_LLM_MODEL=mlx-community/Qwen3-4B-Instruct-2507-8bit
