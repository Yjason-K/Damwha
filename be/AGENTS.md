# AGENTS.md

Guidance for `be/` (package `damwha-be`) lives in [`CLAUDE.md`](CLAUDE.md) in this
directory. Read it as written for any coding agent, not only Claude Code. Keeping a
second copy here let the two drift apart (reaper status propagation, `job.next_attempt_at`
backoff, the worker's process model), so this file only points there.
