# G1 금지 문자열 (스펙 §4.1 표 7행). check-macho.sh가 이 파일만 읽는다.
#
# `#` 주석과 빈 줄은 검사기가 걸러 낸다. 나머지 줄은 grep -F 고정 문자열이다.
# 이 파일은 검사기가 **자기 자신을 검사 대상에서 제외하는 유일한 파일**이다
# (lib/ 전체가 양성 대조군이므로, 패턴 원본이 자기 자신에게 걸리면 안 된다).
# 제외는 이 파일의 절대 경로 하나로만 이뤄지므로 bundle/ 같은 실제 검사
# 대상에서는 어떤 경우에도 발동하지 않는다.
#
# 행 1 — Homebrew 전체. ffmpeg 9.0.1이 여기 있다.
/opt/homebrew
# 행 2 — 이 머신에 실제로 설치된 Python 3.9 / 3.11. dyld가 PATH 없이 찾는다.
#        스펙 §4.1이 "가장 중요하다"고 못 박은 항목.
/Library/Frameworks/Python.framework
# 행 3 — 위 Python.framework, OrbStack docker, ollama의 심볼릭 링크가 사는 곳.
/usr/local/bin
# 행 4 — 현재 비어 있으나 dyld 기본 검색 경로다 (세 문자열).
/usr/local/lib
/usr/local/Cellar
/usr/local/opt
# 행 5 — 개발자 홈 전체. ~/.local/bin의 uv tool과 ~/.cache/huggingface(40 GB)가
#        여기 있다. check-macho.sh는 실행 시점의 실제 홈도 동적으로 덧붙인다.
/Users/gim-yeongjae
# 행 6 — 개발 가상환경 경로. 스펙은 이 행을 "개발 venv 경로"로 정의하고
#        pyvenv.cfg를 "그 경로가 박히는 자리"로 든다. 그래서 금지 문자열은
#        경로 성분이고, pyvenv.cfg는 파일명이라 금지 대상이 아니다 —
#        재배치 가능한 번들도 그 파일을 정상적으로 갖는다.
/.venv
# 행 7 — 빌드 툴체인. 번들 실행에 필요해서는 안 된다.
/Library/Developer/CommandLineTools
